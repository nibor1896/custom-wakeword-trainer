# Torch with CUDA (for GPU). For CPU-only, drop the --extra-index-url line below.
--extra-index-url https://download.pytorch.org/whl/cu130
torch
openwakeword
audiomentations==0.33.0
soundfile
librosa
numpy
requests

# Only needed to generate the synthetic positives (piper-sample-generator).
# webrtcvad does not build on Python 3.13 -> use webrtcvad-wheels (prebuilt wheel).
piper-tts==1.3.0
webrtcvad-wheels
